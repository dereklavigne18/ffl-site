// import { selectStandingsPageDomain } from '../selectors';

describe('selectStandingsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
