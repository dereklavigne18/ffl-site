// import { selectScoreboardPageDomain } from '../selectors';

describe('selectScoreboardPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
